public class Sedan extends Car{

    public Sedan(double tankSize, String name, String numberPlate, String fuelType, int wheelCount, double speed) {
        super(tankSize, name, numberPlate, fuelType, wheelCount, speed);
    }
}
